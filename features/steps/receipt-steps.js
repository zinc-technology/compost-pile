const { Then } = require("cucumber");
Then('they receive a receipt with refund instructions', function () {
  // Write code here that turns the phrase above into concrete actions
  return 'pending';
});